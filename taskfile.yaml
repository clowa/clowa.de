version: "3"

tasks:
  build:
    dir: swa/
    cmds:
      - npm run build

  run:
    dir: swa/
    cmds:
      - npm run dev

  swa:
    deps: [build]
    cmds:
      - |
        swa start \
          --run 'npm run dev' \
          --app-location ./swa/ \
          --api-location ./api/ \
          --output-location dist/
